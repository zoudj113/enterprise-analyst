---
name: enterprise-analyst
description: 企业财报深度分析技能。当用户需要分析企业财务报表、年度报告、招股书、商业计划书，或请求对某公司进行财务体检、护城河研判、风险扫描、业绩归因时触发。支持本地 PDF、ima 知识库与外部获取（A 股巨潮 / 港股披露易 / 美股 EDGAR 一手公告）三种数据源，支持单公司深度分析与多公司对比矩阵，**默认输出单文件 HTML 报告**（含 sticky 侧边目录与 Chart.js 图表），可选 Markdown，成稿后回传 ima 同名知识库。触发词包括：财报分析、年报分析、企业分析、财务体检、招股书分析、商业计划书分析、护城河、资产负债表分析、收入下滑归因、营收为什么下降、RevPAR、毛利率分析等。
agent_created: true
---

# Enterprise Analyst — 企业财报深度分析

## 概述

本技能将 AI 转化为资深企业财务分析师，专门对中国及全球企业的财务报表、年度报告、招股书、商业计划书进行深度分析，输出结构化分析报告。

详细的分析框架、运行准则和期望输出格式，请参考：`references/enterprise_analyst_prompt.md`

## 触发场景

- 用户提供财报文件（PDF/文字内容）请求分析
- 用户询问某企业的财务状况、盈利质量、护城河
- 用户请求对比多家公司的财务表现
- 用户需要风险扫描或投资研判

## 完整工作流

```
用户给数据源
    ↓
[Step 0] 判断数据源：本地 PDF 目录 / ima 知识库 / **都没有 → 分支 C 外部获取**
    ↓
[Step 1] 取文本（本地 PDF 用脚本提取；ima 用 fetch_media_content；分支 C 先下载一手 PDF）
    ↓
[Step 2] 读文本（大文件先切片），按八步法分析
    ↓
[Step 3] 撰写报告（默认 HTML；用户指定时出 Markdown）
    ↓
[Step 3.5] 运行 scripts/check_report.py 自检 —— 全部 PASS 才可交付
    ↓
[Step 3.8] 回传 ima 知识库（同名库 → 同名/最匹配文件夹）；无匹配文件夹则传根目录
    ↓
[Step 4] 用 Python 清理中间文件；present_files 交付
```

### 数据源分支（Step 0/1）

| 数据源 | 取文本方式 |
|---|---|
| **本地 PDF 目录** | `python scripts/extract_pdf_text.py <pdf_dir> <txt_output_dir>`（依赖 pymupdf） |
| **ima 知识库** | ① `get_knowledge_base_list` 定位知识库 id → ② `search_knowledge` 检索 → ③ `fetch_media_content` 按 media_id 取全文 → ④ 结果落在 tool-results 的 `.txt` 里（文件名含时间戳），先 Copy 到工作区再分析。**无需、也不能用** `extract_pdf_text.py` |
| **C · 本地与 ima 都没有** | 见下方「分支 C：外部获取」。**A 股走巨潮**（`scripts/cninfo_fetch.py`）→ PDF → `extract_pdf_text.py` → `ima_doc_prepare.py` |

### 分支 C：本地与 ima 都没有（外部获取）

**触发**：用户要分析的公司，本地无 PDF、ima 也无对应知识库/财报；**或 ima 有但缺最新报告期**（缺期按同一分支补齐）。

**只补缺口时不要跑默认套装**：ima 已有大部分年报、只缺最新一期，跑全套会重复下载。此时用
`--se-date 2026-01-01~2026-12-31` 或 `--type interim --per-type 1` 精准补缺的那一份。

**先问后做（避免白跑）**：能明确公司名 + 上市地时直接开干；若上市地/代码存疑（如同名公司、A+H 双重上市），先用一次工具确认，不要猜。

**① A 股 —— 巨潮资讯网（一手 PDF，首选）**

```bash
# 1) 先干跑，看清会拉什么（强烈建议第一步）
python scripts/cninfo_fetch.py 000807 --list --log _plan.txt

# 2) 正式下载
python scripts/cninfo_fetch.py 000807 --out ./_src --json --log _dl.txt
```

**默认套装**（不传 `--type` 时自动生效）一次拉齐一份公司分析所需的全部一手材料：

| 内容 | 数量规则 |
|---|---|
| 年报 | **近 5 年**（`--years N` 可调）；按标题年份去重，同年只留最新一份 |
| 招股说明书 / 招股意向书 | 全部命中，有多少取多少（`--no-prospectus` 可跳过） |
| 最近一期半年报 | 1 份 |
| 最近一期一季报 | 1 份 |
| 最近一期三季报 | 1 份 |

- 上市不足 5 年 / 老公司招股书未电子化 → **有多少取多少，不报错**（实测：中国移动 A 股仅 5 份年报照样取 5 份；云铝 1998 年招股书可取）。
- 季报取「最近一期已发布」：如 2026-09 分析时 2026Q3 尚未披露，自动回退到 2025 三季报。
- 标准库实现，managed python 即可跑；orgId 自动查询，无需手填。
- 默认排除「摘要」版本（信息量不足），需要时加 `--include-summary`。
- `--type annual,interim --per-type 3` 可切手工模式（传了 `--type` 即退出默认套装）。
- `--log <file>` 把输出写进 UTF-8 文件——**PowerShell 重定向中文会乱码，用它更稳**。

> **坑（实测确认）**：招股说明书**不能用 category 编码**——巨潮对未知 category 会退化成
> 「返回全部公告」（实测 `category_zsg_szsh` 等 8 个候选全部返回 2537 条全量）。
> 脚本改用 `searchkey` 标题关键词检索，并清洗了高亮的 `<em>` 标签。

- 下载后**校验**：脚本会拒绝非 `%PDF` 开头的内容；老公告可能是**扫描件（图片型 PDF）**，
  提取后文本为空——此时换数据源或人工处理，**严禁凭空编造数字**。

**② 港股 —— 用 `hkex-reports-to-ima` 技能**（披露易 hkexnews 搜索 API → PDF 下载 → pypdf 校验）。
该技能原本是「下载 + 上传 ima」，此处只取它的**下载**环节即可，不必上传。

**③ 美股 —— SEC EDGAR**（`https://www.sec.gov/cgi-bin/browse-edgar?action=getcompany&CIK=&type=10-K`），
10-K 为年报、10-Q 为季报；注意财年截止月可能与自然年不同。

**④ 提取与索引**（与本地 PDF 分支相同）
```bash
<venv-python> scripts/extract_pdf_text.py <下载目录> <下载目录>     # 必须 venv（pymupdf）
python        scripts/ima_doc_prepare.py  <下载目录>                # 目录批量模式，推荐
```
> **中文文件名不要从 PowerShell 命令行传给 Python** —— 会被编码打乱报「文件不存在」。
> `ima_doc_prepare.py` 传**目录**（批量模式）即可绕开；`extract_pdf_text.py` 本来就接受目录。

**⑤ 标注来源**：走分支 C 时，报告「数据来源」须写明「取自巨潮资讯网/披露易/EDGAR 一手公告 PDF」，
与 ima 一手原文同等对待（**都是一手**）；只有券商观点、行业价格、行情等非公司披露内容才归入「外部联网数据」。

**ima 长文本处理要点**：年报全文可达数十万字符，**禁止直接 Read 整份**。正确做法是先用 Grep 定位关键词行号，再写一段 Python 脚本按行切片输出到若干小文件（如 `_part1.txt`），逐段 Read。分析完成后必须清理所有中间文件。

> **Windows 清理中间文件必须用 Python `os.remove`**：本机 PowerShell `Remove-Item` 会静默失败（文件仍在），
> Bash `rm` 也可能因 shim 缺 `rm`/`dirname` 报 command not found。用 `.workbuddy/binaries/python` 或其中的 venv 执行。
> **坑**：`shutil.rmtree(dir, ignore_errors=True)` 经常「文件删光但目录壳还在」——删完必须
> `os.listdir` 复核，若目录仍存在再 `shutil.rmtree` 一次或逐层 `os.rmdir`。

### 效率要点（减少轮次 · 2026-09 复盘）

一次完整分析约 60 轮工具调用，其中约 80% 耗在「把非结构化 PDF 变成我能读的片段」。五条硬规则：

1. **Step 0 先确认时间覆盖，开工前一次想清楚要不要补外部数据。** 列出知识库目录后先判断最新一期是哪期；若缺期或整家公司在 ima 都没有，**走分支 C，在开工前一次性「下载 → 提取 → 建索引」**。**不要分析到一半才发现缺期再返工**（教训：云铝 2026 半年报 ima 没有，中途临时手写脚本现凑，单这一项耗 14 轮）。
2. **「取全文 → 清洗 → 建关键词索引」用现成脚本一次跑完**：
   `python scripts/ima_doc_prepare.py <原始.txt> [-o 输出目录] [-k 关键词文件]`
   一次产出 `{名}_clean.md` + `{名}_index.txt`（关键词 → 行号）。**不要现写 probe/conv/idx 三个脚本各跑一轮**。
3. **定位用 Grep 精确命中，不要靠行号盲读。** 长文档先 Grep 关键词拿行号，再 Read 该行号前后 ±N 行。按行号猜测的命中率约五成，会反复补读（本次 2025+2023 两份年报精读共耗 17 轮）。
4. **Windows 命令类操作有隐藏成本**：Bash 常损坏、PowerShell 不回显 stdout，每条命令都要「写 UTF-8 文件 → Read」两次往返，等于命令类操作轮次翻倍。**能合并进 Python 脚本的逻辑一律合并**，不要拆成多条命令。
5. **报告写作一次性写大块**：HTML 报告分 4+ 次 Edit 插入章节会显著增加轮次，尽量 2 次以内（骨架 + 主体），最后单独补图表脚本与导航。

### 输出格式与命名（Step 3）

**默认输出 HTML**（单文件）。仅当用户明确要 Markdown 时才出 `.md`。

**文件命名（固定规则，不得自行变通）**：

```
{公司简称}_分析报告_{YYYYMMDD}.html
```

- 日期为**报告生成当天**的本地日期，8 位无分隔符；用 `date +%Y%m%d` 获取，禁止心算或凭印象写。
- 示例：`中烟香港_分析报告_20260917.html`、`农夫山泉_分析报告_20260917.html`
- Markdown 分支同规则换扩展名：`{公司简称}_分析报告_{YYYYMMDD}.md`
- 同一天对同一公司出第二版（如补充章节）时追加版本号：`中烟香港_分析报告_20260917_v2.html`

- **HTML**：生成单文件 HTML，**直接复用 `references/html_report_template.md` 的骨架与 CSS**，不要从零写样式。
  **章节结构、表格同比列、护城河 ★ 评级、每章末尾数据来源注释、文末会计准则与免责声明必须全部保留**——
  HTML 只是渲染形式，不得削减内容。
- **HTML 必须带固定侧边目录**（详见下方「导航要求」）：左侧栏 `position:sticky`，页面滚动时目录保持可见，
  不可随内容滚走。
- **两种格式均需遵守「时间列排序规则」**（见「格式要求」章节）：含年份/期间表头的表格与图表，最新期间排最左。

### 导航要求（HTML 必做）

长报告必须在左侧放**常驻目录**，满足「向下滑动后仍能一键跳转」：

1. **布局**：`.layout` 栅格 = 侧栏 212px + 正文 `minmax(0,1fr)`；正文必须设 `min-width:0`，否则宽表格会撑破栅格。
2. **常驻**：侧栏 `position:sticky; top:24px; max-height:calc(100vh - 48px); overflow-y:auto`（章节超一屏时目录自身可滚）。
   祖先元素**不得**出现 `overflow:hidden`，否则 sticky 失效。
3. **锚点**：每个 `<h2 id="sN">` 与目录 `<a href="#sN">` 一一对应；未被编号的前言章节（如「核心速览」）也要给 id 并纳入目录。
4. **跳转留白**：`h2{scroll-margin-top:20px}`，避免锚点贴顶。
5. **滚动高亮（建议）**：用 IntersectionObserver 或 scroll 监听给当前章节加 `.active` 类指示位置。
6. **窄屏降级**：`max-width:1080px` 时栅格塌为单列、侧栏还原为静态双列目录置于正文顶部。
7. 目录底部放「回到顶部」链接，`<header id="top">` 提供锚点。

> **Markdown 分支**：Markdown 无法做 sticky 侧栏，退化为文首普通目录列表即可。

### 交付前自检（Step 3.5，必做）

```bash
python scripts/check_report.py <报告文件.html|.md>
```

覆盖 8 类检查：HTML 标签闭合 → 表格列数一致性 → canvas 与图表注册配对 → **时间列排序规则** →
必备章节完整性 → 数据来源与免责声明 → **第 0 层硬伤核验痕迹（须写明审计意见，不得凭印象）** →
常驻侧边目录。退出码 0 才可交付，有 FAIL 必须修完再交付。

> 已知豁免：**瀑布图**等流程型图表的横轴是推导路径而非并列期间，不受「最新在最左」约束，
> 但须在图表副标题注明。脚本对图表 X 轴方向只提示不判 FAIL。

### 回传 ima 知识库（Step 3.8，报告自检通过后执行）

把成稿回传到 ima 同名知识库，便于后续检索与追问。**数据源取自 ima 时必定执行**；数据源为本地 PDF
且用户未要求时，可询问是否回传。

**① 定位知识库**：`mcp__ima-mcp__get_addable_knowledge_base_list`（limit 50）拿可写库列表，
按企业简称**精确匹配**名称取 `knowledge_base_id`。

**② 定位文件夹**：`get_knowledge_list`（`knowledge_base_id` + limit，不带 `folder_id` 即列根目录）
→ 取 `media_type=99` 的条目（`folder_info.folder_id` / `.name`）。匹配优先级：
企业同名文件夹 → 名称含「分析/研报/报告」的文件夹 → 无匹配则传**知识库根目录**（不传 `folder_id`）。

> **硬限制（必读）**：ima MCP **没有创建文件夹或知识库的接口**（工具仅
> `get_*_knowledge_base_list` / `get_knowledge_list` / `add_knowledge` / `create_media` /
> `fetch_media_content` / `search_*` / `import_urls`）。因此「没有文件夹就新建」**做不到**。
> 遇到无匹配文件夹时：上传到根目录，并在回复里明确告知用户——**禁止声称已创建文件夹**；
> 可建议用户在 ima 客户端建好后把 folder_id 给我，再补传/移动。

**③ 上传三步**（配方已在 hkex-reports-to-ima 技能中验证，2026-09 云铝报告实测通过）：

1. `create_media`：`knowledge_base_id` + `file_name`(含扩展名) + `file_ext` + `file_size`
   (**必须为精确字节数**，服务端校验，超限直接拒绝) + `content_type`(按扩展名精确填：
   `html→text/html`、`md→text/markdown`、`pdf→application/pdf`，禁止 octet-stream 兜底)。
   返回 `media_id` + `cos_credential`（token/secret_id/secret_key/bucket/region/cos_key，有效期 12h）。
2. COS 上传：**直接用现成脚本**（venv python，已装 `cos-python-sdk-v5`）：
   ```
   <venv-python> scripts/ima_upload_cos.py --cred cred.json --file <报告.html> --result out.txt
   ```
   `cred.json` 存 `create_media` 返回的 `cos_credential` 原样 JSON；结果写进 `--result`（PowerShell 不回显 stdout）。
   等价的手写版本（脚本内部实现）供参考：
   ```python
   from qcloud_cos import CosConfig, CosS3Client
   cfg = CosConfig(Region=region, SecretId=sid, SecretKey=skey, Token=token, Scheme="https")
   CosS3Client(cfg).put_object_from_local_file(
       Bucket=bucket, LocalFilePath=local, Key=cos_key, ContentType=content_type)
   ```
   **坑**：参数名是 `LocalFilePath`，不是 `Body`；凭证每个文件各不相同，需逐个 `create_media`。
3. `add_knowledge`：`knowledge_base_id` + `media_id` + `folder_id`(有则传) +
   `duplicate_name_strategy=DUPLICATE_NAME_STRATEGY_SAVE`。

**④ 校验**：再次 `get_knowledge_list` 确认条目存在、`file_size` 与本地一致、
`media_state=1`（解析中）或 `2`（解析完成）。

**⑤ 收尾**：回复中给出「已上传至 ima 知识库《X》→ 文件夹《Y》」；若走了根目录降级，一并说明原因。

> **本机环境坑**：Bash 常损坏（用 PowerShell）；PowerShell 不回显 stdout（脚本写 UTF-8 文件再 Read）；
> 取字节数用 Python `os.path.getsize`，**不要**用字符数估算（中文 3 字节，会与服务端校验不符）。
>
> **HTML vs Markdown**：HTML 入库类型为 HTML，RAG 检索效果不如 Markdown。若用户主要是为了
> 后续检索问答，建议额外生成一份 `.md` 一同入库（`text/markdown`），HTML 保留给人看。

## PDF 文本提取（Step 1 的本地分支）

当用户提供 PDF 财报文件时，必须先将其提取为纯文本再分析。使用 skill 内置脚本：

```bash
# 用法 1：处理目录下所有 PDF
python scripts/extract_pdf_text.py <pdf_dir> <txt_output_dir>

# 用法 2：指定特定文件
python scripts/extract_pdf_text.py <pdf_dir> <txt_output_dir> --files 文件名1.pdf 文件名2.pdf
```

**示例**：
```bash
python scripts/extract_pdf_text.py \
  "C:\Users\zx\Desktop\C\N农夫山泉\workbuddy" \
  "C:\Users\zx\Desktop\C\N农夫山泉\workbuddy"
```

**说明**：`txt_output_dir` 可以与 `pdf_dir` 相同（PDF 和 TXT 放在一起），也可以分开。

**输出**：每个 PDF 对应一个 `{原名}_pymupdf.txt` 文件，UTF-8 编码，每页以 `--- Page N ---` 分隔。

**依赖**：需先安装 `pymupdf`：`pip install pymupdf`

> **解释器必须用 venv（本机已验证 2026-09）**：`pymupdf` 只装在隔离 venv 里，managed 版 Python 没有。
> 直接 `python scripts/...` 会报 ModuleNotFoundError。正确调用：
> ```
> "C:\Users\zx\.workbuddy\binaries\python\envs\default\Scripts\python.exe" "C:\Users\zx\.workbuddy\skills\enterprise-analyst\scripts\extract_pdf_text.py" <pdf_dir> <txt_output_dir>
> ```
> 装包走清华源：`"...\envs\default\Scripts\pip.exe" install pymupdf -i https://pypi.tuna.tsinghua.edu.cn/simple`
> venv 里已有：pymupdf、pypdf、edge-tts、imageio-ffmpeg、cos-python-sdk-v5。
> 注：Bash 工具常损坏（`dirname`/`head` not found），用 PowerShell 调用；PowerShell 不回显 stdout，
> 需要输出时让脚本写 UTF-8 文件再用 Read 读。

## 分析执行（Step 2）

读取 Step 1 得到的文本，严格按**八步法**分析（详见「分析流程」章节）。

**重要**：切勿直接读取 PDF（PyMuPDF 文本提取已证明优于直接读取 PDF）。

## 报告撰写与保存（Step 3）

分析完成后保存报告。**报告保存目录（可配置）**：

| 模式 | 说明 |
|------|------|
| **默认**：与数据源同目录 | 本地 PDF 模式保存在 `pdf_dir`；ima 模式保存在用户提供或未指定时的当前工作区 |
| **自定义目录** | 用户可指定任意目录，通过参数或对话确认。用户若指定了文档目录，必须落在那里，不要放进 `.workbuddy/` |

**文件命名规则（先在终端 `date +%Y%m%d` 取当天日期）**：`{公司简称}_分析报告_{YYYYMMDD}.{html|md}`

- HTML 示例（默认）：`中烟香港_分析报告_20260917.html`
- Markdown 示例（用户指定时）：`农夫山泉_分析报告_20260917.md`
- 同日同公司的修订版加后缀：`中烟香港_分析报告_20260917_v2.html`
- ⚠️ 不得再使用 `{公司名}_{报告类型}_深度分析报告.{md|html}` 的旧命名。

> 八步法的**分析顺序不等于输出章节顺序**。二者的对应关系见「输出规范」末尾的映射表。

## 分析流程（八步法）

**前置：市场与会计准则判定（必做，先于所有比率计算）**

上市地 → 会计准则（CAS / HKFRS / US GAAP）→ **所有判读基准随之调整**。基准选错，后面整篇结论都会错。

| 维度 | A 股 | 港股 | 美股 |
|---|---|---|---|
| 准则 | CAS | HKFRS/IFRS | US GAAP（部分 IFRS） |
| 增值税 | 有（13%） | **无** | 无 |
| 季报 | 强制 | 无强制 | 强制（10-Q） |
| 存货计价 | 禁用 LIFO | 禁用 LIFO | 允许 LIFO |
| 研发支出 | 满足条件可资本化 | 开发阶段可资本化 | **全部费用化** |
| 资产减值转回 | 特定条件下可转回 | 可转回（商誉除外） | **不可转回** |
| 财年截止 | 自然年 | 自然年 | **可自选月份** |

**港股两条硬纪律**
① **无增值税** → 销售收现比基准取 **1.0**，不得套用 A 股的 1.13。
② 披露粒度粗于 A 股 → 科目缺失时写"因港股披露粒度差异无法深入分析"，**不得标记为财务疑点**，也不得填 0 估算。

**美股注意**：分析前必查财报期起止日期；研发费用化使科技/医药公司研发费率系统性偏高，勿误报；
LIFO 公司存货账面值与毛利率不可与 A/H 股同行直比；审计意见按 PCAOB 英文术语识别
（Unqualified/Qualified/Adverse/Disclaimer），**关键审计事项（KAM）是排雷高价值线索**；
中概股额外关注 VIE 结构、审计底稿争议、SEC 问询函、Form 4 内部人减持。

---

### 第一步：业务拆解
分析企业核心业务与边缘业务，计算各板块营收占比及同比增速，判断利润含金量（净利润 vs 经营性现金流）。

### 第二步：财务体检与经营数据分析
重点追踪毛利率、费用率（销售/管理/研发）、经营性现金流净额趋势；拆解各业务板块的量、价、结构。

**利润与现金流的必算口径（补充「经营现金流 / 净利润」的不足）**：参见
`references/forensic_risk_layer.md` 第二、三节 ——

- **利润结构四分法**：把利润定性为「干出来的 / 补出来的 / 投出来的 / 洗出来的」之一，
  不能只说"非经常性损益占 X%"。
- **核心利润获现率** = 经营现金流净额 ÷ 核心利润（连续 2 年 <50% 红灯）。
- **销售收现比** = 销售收现 ÷ 营业收入，**基准按上市地选**：A 股 1.13（含增值税）、港股/美股 **1.0**。
- **现金流量肖像八型**（CFO/CFI/CFF 符号组合）+ 近 3 期漂移路径。

**经营变动归因（触发即必做）**：收入、毛利、净利润任一同比变动超过 **±10%** 时，不得只陈述涨跌，
必须完成「分部贡献表 + 量价分解 + 归因瀑布图 + 可修复性排序表」四件套，并就可能涉及宏观/行业变量的
归因做**外部交叉验证**。<b>±10% 以内也须用一句话点明主因。</b>

方法与版式详见 `references/attribution_analysis.md`。

**时间列排序规则**（见「格式要求」）在本步生成的分部表中同样适用。

### 第三步：资产负债表分析（必做）
把合并资产负债表压缩为**五类科目**，绘制「简易合并资产负债表」，并做专项与效率分析：

- **资产端五类**：类现金 → 投资资产 → 应收款项 → 经营资产 → 其他资产（含即期可收回税项）
- **负债及权益端四类**：有息负债 → 经营性负债 → 其他负债 → 净资产
- **必做的专项科目**：
  1. **应收账款**：贸易应收 + 应收票据，账龄结构（30/90 日以上占比）、逾期分布、是否计提减值、关联方占比；与收入增速是否背离
  2. **合同负债（合约负债）**：金额与变动，作为未来收入蓄水池的意义
  3. **有息负债**：长短期借款拆分、利率、抵押/担保、到期结构；与类现金对比（净现金 or 净负债）
- **必算的效率指标**：应收/存货/应付周转天数、现金转换周期 CCC = 应收天数 + 存货天数 − 应付天数、类现金 ÷ 有息负债、净现金、有息负债 ÷ 权益总额
- **平账要求**：左右两侧合计必须相等；分项与合计的 ±0.01 尾差需注明为四舍五入所致
- **表格与模板**：见 `references/simple_balance_sheet.md`（含科目归类映射表与 Markdown / HTML 两种版式）

### 第四步：股权架构分析
梳理公司沿革与股本演变（成立、改制、历次增资、IPO、配售、股权激励、回购等），绘制当前股权结构（控股股东、持股比例、控制路径），列示主要子公司及持股比例，评估控股结构特点（如家族控股、国资背景、海外架构等）。

**可视化输出**：完成股权架构分析后，应根据 Markdown 渲染环境选择方案：

- **方案 A · 纯文本 ASCII Art（推荐）**：使用 ` ` 代码块包裹的 ASCII 字符图（`┌─┐│└┘▼` 等），**100% 兼容所有渲染器**（VS Code、Typora、GitHub、Notion、Obsidian、WorkBuddy），且能精确复刻招股说明书原图。详见 `references/equity_structure_ascii.md`。
- **方案 B · Mermaid 流程图**：使用 ` ```mermaid ` 代码块 + `flowchart TD` + `classDef`，渲染更精美但部分渲染器可能失败。详见 `references/equity_structure_mermaid.md`。

**优先选择方案 A（ASCII）**。仅在确认渲染器支持 Mermaid 时才用方案 B。

**ASCII Art 绘制规范**：
- 三层结构：实控人（顶层）→ 上市公司（中层）→ 主要子公司（底层）
- 用 `┌─┐` `└─┘` `│` `▼` `┬┴├` 等字符绘制树状结构
- 上下层级用 `│` 垂直连接，平行分支用 `┌┐└┘─` 水平连接
- 控股方框用 `┌─┐` 包裹强调（如"控制合计 36.47%"）
- 100% 全资直接列出，部分持股标注百分比
- 图下方用 `(1)(2)(3)(4)` 编号 + 列表说明复杂股权关系
- 颜色用文字标注（"紫色=控股股东"），不依赖 CSS

**股权架构图绘制规范**：
- 采用三层结构：控股股东层（顶层）→ 上市公司（中层）→ 主要子公司（底层）
- 使用箭头标注持股比例（如"持股 59.21%"、"直接 2.80%"）
- 颜色编码区分层级：控股股东用紫色/蓝色，上市公司用琥珀色，境内子公司用青色，境外子公司用珊瑚色，合营公司用琥珀色，公众股东用灰色
- 控股平台（如家族投资公司）单独节点标注
- 库存股等特殊安排用虚线框标注
- 底部注释说明颜色含义和数据来源
- SVG viewBox 固定为 `0 0 680 H`，H 根据内容自适应

### 第五步：融资与分红情况分析
梳理历次股权融资（IPO、增发、配售、债券）及募资用途，分析分红政策与历史分红记录（支付率、分红金额、稳定性），评估融资与分红的匹配度（融资效率、分红覆盖能力、资本结构优化、股东回报诚意）。

### 第六步：护城河研判
根据市场地位、技术壁垒、特许权、用户粘性评估护城河深度，使用 ★ 星级评定。考察盈利可持续性（复购率、行业需求、竞争格局）和资本投入依赖度（CAPEX/OCF）。

### 第七步：风险扫描（三层结构，必做）

风险清单**必须分三层，硬伤否决在前、企业自身次之、外部环境最后**。
**只写宏观风险（政策、地缘、汇率、行业周期）属于不合格**——那是"公司承受的风险"，分析的价值在于找出"公司自己造出来的风险"。

**第 0 层 · 硬伤一票否决（前置，必做）**

在做任何比率分析之前，先核 8 项硬伤：**审计意见类型 / 关键审计事项（KAM）/ 审计师变更 /
控股股东质押 >50% / 监管问询与做空报告 / 推迟刊发业绩 / 内部人减持 / 离岸主体畸高**。
全部无异常压缩为一句话写进风险章节开头；命中非标审计意见时须在「核心速览」顶部加醒目提示。

⚠️ **不得凭印象写"无审计保留意见"** —— 必须回原文核实后才写（历史教训：中烟香港报告曾未查即写，属虚假陈述）。
完整清单与取证方法见 `references/forensic_risk_layer.md`。

**A. 企业自身层面（财务结构与经营质量，10 项必查）**

| # | 检查项 | 判定阈值 / 要点 | 数据来源 |
|---|---|---|---|
| 1 | **关联交易依赖** | 关连交易收入/采购占比；**是否获豁免年度上限**（无上限 = 定价公允性完全依赖内部机制，风险更高）；关联方应收/应付占该科目比重 | 年报/中报「持续关连交易」章节 + 附注「重大关联方交易」 |
| 2 | **集中度** | 单一业务 >50%、单一客户 >30%、单一市场 >50% 需列为高风险 | 附注「收入及分部报告」地区资料 |
| 3 | **营收 / 毛利 / 净利骤降** | 任一指标同比跌超 **20%** 必须单列，并拆到分部定位降幅来源 | 财务摘要 + 分部业绩表 |
| 4 | **非经营利润占比过大** | 其他收入净额（利息+汇兑+补助+处置）÷ 税前利润：**>10% 黄灯、>20% 红灯**；并反算「剔除汇兑后的利润增速」 | 综合损益表 + 附注「其他收入净额」 |
| 5 | **存贷双高** | 类现金与有息负债同时高企时，**必须比较存款年化收益率与借款利率是否倒挂**；再判断是「跨境/并表层级分割」还是「真实性存疑」 | 财务状况表 + 附注「现金及银行存款」「银行借款」 |
| 6 | **毛利率异常改善** | 区分「经营效率提升」与「**低毛利业务萎缩更快的混合效应**」；后者明确标注<b>不可外推</b> | 分部收入/毛利表 |
| 7 | **应收 / 存货 / 应付与收入背离** | 应收增速 − 收入增速 **>30pp 亮红灯**；查账龄迁移、逾期分布、**减值计提是否充分**（长期零计提 = 会计偏乐观） | 附注「贸易及其他应收款项」账龄与逾期分析 |
| 8 | **商誉与长期资产减值** | 商誉占净资产比例；收购以来是否**零减值**；标的最新经营波动 | 财务状况表 + 附注「无形资产及商誉」 |
| 9 | **资本配置效率** | 募资使用进度、闲置比例、承诺兑现度（如 IPO 承诺并购） | 「所得款项用途」表 |
| 10 | **少数股东损益分流** | 非控股权益损益占净利润比例变化（并表子公司非全资时） | 综合损益表下方分配 |

**B. 关键数据异常变动监测表（必做）**
表格列固定为：`指标 | 本期（最新） | 上期 | 变动 | 判定（异常/关注/良性/改善）| 说明`（遵守时间列排序规则，最新在左）。至少覆盖 10 项，必须混合异常项与良性项（只列异常会误导）。

**C. 外部环境与治理层面**
政策与监管、地缘政治、汇率、气候/农产品、治理与人事、税率变化等。

**输出要求**
- 严重程度用 🔴 高 / 🟡 中 / 🟢 低；**每条必须带具体数字与出处**，禁止纯定性描述。
- **避免重复**：同一风险只出现在一处（如"应收账款"归入企业自身层面，外部表不再重复）。
- **必须给出明确结论**：末尾写明"是否发现典型财务造假红旗"，并一句话概括利润质量走向。
- 详见 `references/risk_checklist.md`

### 第八步：输出报告
按「输出规范」章节的要求，生成完整报告（九个必备章节，可按需扩展）并以约定格式保存文件；
随后**必须执行 Step 3.5 自检**。

## 输出规范

报告须包含以下**九个必备章节**（这是下限，允许按标的情况扩展更多章节）：

> 实践中通常扩展为 11 章：开头加「核心速览」（KPI 卡片，6–8 个关键指标一眼看全），末尾加「市场定价与估值」
> （收盘价、PE/PB/股息率、52 周位置、卖方一致预期）。这两章虽非必备，但**除非用户明确不要，建议包含**。

1. **企业概况** — 公司全称、上市地及股票代码、业务模式、行业地位、当前规模、发展脉络
2. **股权架构** — 公司沿革与股本演变、当前股权结构（控股股东及持股比例）、子公司架构
3. **融资与分红情况** — 历次股权融资及募资用途、分红政策与历史分红、融资与分红匹配度分析
4. **核心财务指标表** — 营业收入、毛利率、经营利润率、净利润率、EBITDA、经营性现金流、费用率拆解（必含同比变化列）
5. **资产负债表分析** — **简易合并资产负债表**（资产五类 / 负债及权益四类，至少两期对照且左右平账）+ **科目明细拆解表** + **应收账款 / 合同负债 / 有息负债专项分析** + **营运资本效率与偿债安全垫指标表**（周转天数、CCC、净现金等）。模板见 `references/simple_balance_sheet.md`
6. **经营数据分析** — 业务板块拆解、运营指标趋势；**如触发归因条件则包含归因四件套**（见第二步）
7. **护城河研判** — 品牌效应、成本优势、网络效应、转换成本（星级评定）
8. **风险预警清单** — **三层结构（必做）**：章首先放一句「**第 0 层硬伤核验结论**」（8 项：审计意见 / 关键审计事项 / 审计师变更 / 控股股东质押>50% / 监管问询与做空 / 推迟刊发 / 内部人减持 / 离岸主体畸高；无触发时压缩为一句话）→ 8.1 企业自身层面（10 项必查：关联交易、集中度、业绩骤降、非经营利润占比、存贷双高、毛利率幻觉、应收背离、商誉减值、资本配置、少数股东分流）→ 8.2 关键数据异常变动监测表（≥10 项量化对照）→ 8.3 外部环境与治理层面。表格含风险类别、描述与量化依据、严重程度（🔴🟡🟢），末尾须给出"是否存在财务造假红旗"的明确结论。模板见 `references/risk_checklist.md`，取证层见 `references/forensic_risk_layer.md`
9. **总结** — 核心亮点与隐忧，3-5 条要点，并给出跟踪这家公司最该看的 2–3 件事

### 分析步骤 ↔ 输出章节映射

两者**顺序不同，这是有意为之**（先排雷再讲钱，但报告先讲家底）。撰写时按下列对应搬运：

| 分析步骤 | 落到输出章节 |
|---|---|
| 第一步 业务拆解 | ① 企业概况 + ⑥ 经营数据分析（板块部分） |
| 第二步 财务体检与归因 | ④ 核心财务指标表 + ⑥ 经营数据分析（归因部分） |
| 第三步 资产负债表分析 | ⑤ 资产负债表分析 |
| 第四步 股权架构分析 | ② 股权架构 |
| 第五步 融资与分红 | ③ 融资与分红情况 |
| 第六步 护城河研判 | ⑦ 护城河研判 |
| 第七步 风险扫描 | ⑧ 风险预警清单 |
| — （综合） | ⑨ 总结、核心速览、市场定价与估值 |

## 格式要求

### 时间列排序规则（强制）

**所有按年份 / 期间显示的表格，最新期间必须排在最左侧，向左递减到最旧期间。**

```
正确：  2026H1 | 2025 | 2024 | 2023
错误：  2023 | 2024 | 2025 | 2026H1
```

适用范围（凡是表头里出现年份或期间的，一律适用）：

| 类型 | 示例表头（正确） |
|---|---|
| 年度 / 中期序列 | `2026H1 \| 2025 \| 2024 \| 2023` |
| 跨年混合期间 | `2026H1 \| 2025H1 \| 2025 \| 2024` |
| 半年 vs 全年混合 | `2026H1 \| 2025H1 \| 2025 全年 \| 2024 全年` |
| 简易资产负债表 | `2026-06-30 \| 2025-12-31 \| 2024-12-31` |
| 预测期 | `2026E \| 2027E \| 2028E`（预测序列按由近到远自左向右递增亦可，但已实现期间永远在最左） |
| 纵向时间序列（时间列在最左侧列，如发展脉络、里程碑表） | 行自**上而下**递减：`2026 年 7 月 | 2026 年 1 月 | 2025 年 | …` |
| 存量流水表（募资使用进度等） | 最新时点余额列置最左：`2026/6/30 未使用 \| 期内已使用 \| 2026/1/1 未使用`，并在表下注明读法 |

执行细则：

1. **两期/三期对照表同样适用**：如分部「本期 / 上期」必须写成 `2026H1 | 2025H1`，不得写成 `2025H1 | 2026H1`。
2. **图表同样适用**：柱形图 / 折线图 / 堆叠图的 X 轴、图例顺序必须自左向右为「最新 → 最旧」，或从旧到新的自然时间轴；同一份报告内**图表与表格的方向必须保持一致**。
3. **变动列紧跟被比较的两个期间**：如 `2026H1 | 2025H1 | 同比变动`；若另设多期，则写 `2026H1 | 2025H1 | 变动 | 2025 全年`。
4. **"回望型"表述不得破坏物理顺序**：即使正文按「从旧到新」叙事，表格本身仍按最新在最左排列；可在表下注明"（按最新期间优先排列）"。
5. **检测清单**：报告写完后逐一检查每张含年份表头的表格与每个含时间轴的图表，凡首列/最左标签不是最新期间的，必须翻转。

### 其他格式要求

- 所有财务数据用 Markdown 表格，必含同比/环比变化列
- 大数值使用"亿""万"等中文单位，同时保留原始精确值
- 若数据未披露，标注"数据未披露"而非推测
- 若源文件无毛利润，根据营收减营业成本推算，并注明"推算"
- 报告末尾注明数据来源和会计准则；**外部联网数据须单列一项**，并注明"非公司披露内容，仅用于交叉验证"，与公司一手披露数据严格区分
- 多份财报分析（同一公司多年）时按年份排成时间序列；**多家公司对比时**在财务指标与护城河章节后追加一张**横向对比矩阵**（行=公司，列=核心指标，最新期在最左），并在总结中给排序结论

## 资源文件

**规则优先级**：本 SKILL.md 是**唯一规则源**。`references/` 下的文件只放模板、版式与操作细节，
不得重复定义或修改规则；references 与本文冲突时**一律以本文为准**。
（历史教训：时间列排序规则曾在 6 处重复定义，每次升级必须同步改 6 个位置，极易漏改。）

- `scripts/extract_pdf_text.py` — 通用 PyMuPDF PDF 文本提取脚本，支持中文财报，支持命令行参数
- `scripts/cninfo_fetch.py` — **A 股财报公告检索/下载**（Step 0 分支 C）：巨潮资讯网 API，按代码+类别拉一手公告 PDF。标准库，managed python 可跑
- `scripts/ima_doc_prepare.py` — **长文档预处理**（Step 1）：清洗 `fetch_media_content` 落地的 JSON/转义文本 → `_clean.md`，并生成 `_index.txt` 关键词行号索引。**支持传目录批量处理（推荐，避开中文名传参问题）**。仅用标准库
- `scripts/ima_upload_cos.py` — **ima 上传第二步**（Step 3.8）：读 `create_media` 凭证 JSON → 上传 COS → 结果落盘。需 venv python（`cos-python-sdk-v5`）
- `scripts/check_report.py` — **交付前自检脚本（Step 3.5 必跑）**：标签闭合 / 表格列数 / canvas 注册 / **时间列排序** / 必备章节 / 数据来源与免责。退出码 0 才可交付
- `references/enterprise_analyst_prompt.md` — 完整分析提示词与示例输出节选（**示例表头已遵守时间列规则**）
- `references/html_report_template.md` — **HTML 单文件报告模板（默认输出）**：完整 CSS、两栏布局、**sticky 侧边目录 + 滚动高亮脚本**、配色语义、常用图表片段。写 HTML 报告时直接复用，不要从零写样式
- `references/attribution_analysis.md` — **经营变动归因方法**（触发阈值 ±10%、四件套、连环替代法、可修复性分档、外部交叉验证路径与标注规则）
- `references/equity_structure_mermaid.md` — 股权架构图 Mermaid 流程图模板（图形化 + 颜色编码）
- `references/equity_structure_ascii.md` — 股权架构图纯文本 ASCII Art 模板（**兼容性最强**，适合所有 Markdown 渲染器）
- `references/simple_balance_sheet.md` — **简易合并资产负债表**模板（五类压缩法、科目归类映射、Markdown/HTML 版式、平账与效率指标口径）
- `references/risk_checklist.md` — **风险扫描清单**（企业自身 10 项必查 + 量化阈值 + 异常变动监测表 + 数据来源定位 + 常见误判 + Markdown/HTML 版式）
- `references/forensic_risk_layer.md` — **舞弊取证增强层**：第 0 层硬伤一票否决 8 项、利润结构四分法、核心利润获现率 / 销售收现比（分市场基准）/ 现金流量肖像八型、勾稽五闸、**判读纪律（信号≠判决）**、6 条补充误判、非财务信号八问、本章交付自检清单
